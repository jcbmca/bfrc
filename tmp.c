

//    char skey[64];
//    unsigned int key;




/*
      case 'r':
            fdb = 1;
            strcpy(skey, (*++argv));
            key = atoi(skey);
            --argc;
            break;
*/


    /*
    if (fremove) {
        raiz = load_file_to_tree( fdb ? db : BASE_DATO );
        nodo = search(raiz, key);
        if (nodo != NULL) {
            nodo = delete_node(&raiz, value);
            printf("Ok:%d Delete\n", nodo->key);
         } else {
            printf("Error:Clave no encontrada.\n", hmac);
        }
        delete_tree(&raiz);
        return 0;
    } */


